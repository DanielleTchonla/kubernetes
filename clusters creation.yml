#eksctl create cluster (to run on cli)s

INSTALL KUBECTL
eksctl create cluster \
--name my-first-cluster \
--version 1.21 \
--region us-east-1 \
--nodegroup-name workers-nodes \
--nodes 3 \
--nodes-min 2 \
--nodes-max 3 \
--managed \